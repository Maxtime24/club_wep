'use client'; import { useEffect, useState } from 'react'; import { supabase } from '@/lib/supabaseClient'; import ProjectCard from './ProjectCard'; export default function TopProjectsClient() { const [projects, setProjects] = useState<any[]>([]); useEffect(() => { async function fetchProjects() { const { data } = await supabase.from('projects').select('*').order('id', { ascending: false }).limit(3); setProjects(data || []); } fetchProjects(); }, []); return ( <div className="grid grid-cols-1 md:grid-cols-3 gap-8"> {projects.map((p) => <ProjectCard key={p.id} project={p} index={0} delay={0} />)} </div> ); }